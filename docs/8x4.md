# If Jane Street unlocks 8×4 tiles (~30% more area)

Keep the ISA unchanged. Spend the extra tiles on, in order:

1. IHP 512×16 SRAM (`src/cycle_sram_ihp.v`) as instruction + capture memory.
2. Capture depth 512 samples with timestamps (cycle counter in the high byte).
3. A third state machine for sniff+inject (MITM) without stealing SM0/SM1.
4. Hardware Manchester helper for 10BASE-T PCS (board still needs magnetics).

Do not grow the flop IMEM; 128 words already cover UART/SPI/I2C/JTAG/SWD.
